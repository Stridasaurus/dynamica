export default function SiteFooter() {
  return (
    <footer className="border-t border-gray-200 dark:border-gray-800 mt-8">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between text-xs text-gray-400 dark:text-gray-600">
        <span>Dynamica — research tools, not financial advice</span>
        <span>Market data via yfinance &amp; public APIs</span>
      </div>
    </footer>
  );
}
